const INITIAL_STATE = {
  name: '',
  gravatarEmail: '',
  assertions: 0,
  score: 0,
};

const player = (state = INITIAL_STATE, action) => {
  switch (action.type) {
  // case :
  //   return {
  //     ...state,
  //   };

  default:
    return state;
  }
};

export default player;
