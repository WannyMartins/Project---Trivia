const INITIAL_STATE = {
};

const token = (state = INITIAL_STATE, action) => {
  switch (action.type) {
  // case value:
  //   return {
  //     ...state,
  //   };

  default:
    return state;
  }
};

export default token;
